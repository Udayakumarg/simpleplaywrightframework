{
  "dev":  { "users": { "admin": { "username": "${ADMIN_USERNAME}", "password": "${ADMIN_PASSWORD}" } } },
  "qa":   { "users": { "admin": { "username": "${ADMIN_USERNAME}", "password": "${ADMIN_PASSWORD}" } } },
  "prod": { "users": { "admin": { "username": "${ADMIN_USERNAME}", "password": "${ADMIN_PASSWORD}" } } }
}
